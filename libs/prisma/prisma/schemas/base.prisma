// --------------------------------------------- //
// Base schema (configurated by the developper)  //
// --------------------------------------------- //

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
    output   = "../../../node_modules/.prisma/client"
}
