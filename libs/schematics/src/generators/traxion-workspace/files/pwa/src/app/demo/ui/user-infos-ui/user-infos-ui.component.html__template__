<div class="user__infos__wrapper">
  <h3 class="user__infos__wrapper__title">Utilisateur connecté:</h3>
  <p>{{ name }}</p>
  <p>Email : {{ email }}</p>
  <p>Rôle : {{ role }}</p>

  <p>
    <button
      nz-button
      nzType="primary"
      class="btn--primary"
      (click)="logoutEvent.emit()"
    >
      Déconnexion
    </button>
  </p>
</div>
