{
  "$id": "Release",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "project": {
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "description": "Nx project name",
      "type": "string",
      "x-prompt": "What project would you like to add release feature (keep it empty if you want the sync release) ?"
    },
    "skipInstall": {
      "$default": false,
      "description": "Skip install",
      "type": "boolean"
    }
  },
  "required": [],
  "title": "",
  "type": "object"
}