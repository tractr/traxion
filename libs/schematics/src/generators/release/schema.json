{
  "$id": "Release",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "project": {
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "description": "Nx project name",
      "type": "string",
      "x-prompt": "What project would you like to add release feature ?"
    }
  },
  "required": ["project"],
  "title": "",
  "type": "object"
}
