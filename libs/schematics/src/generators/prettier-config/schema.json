{
  "$id": "Prettier",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "format": {
      "default": false,
      "description": "Format with prettier add the end of the schematics",
      "type": "boolean"
    },
    "skipInstall": {
      "default": false,
      "description": "Skip installing dependencies",
      "type": "boolean"
    }
  },
  "required": [],
  "title": "Prettier configuration schematics",
  "type": "object"
}