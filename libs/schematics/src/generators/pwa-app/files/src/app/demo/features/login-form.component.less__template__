.page__wrapper {
  @apply max-w-7xl mx-auto px-2 pb-6 sm:px-6 lg:px-8 mt-4;

  .api__wrapper {
    @apply mt-6 grid grid-cols-1 md:grid-cols-2 gap-6;
    &__item {
      &__title {
        @apply text-2xl font-bold;
      }
    }
  }
}
