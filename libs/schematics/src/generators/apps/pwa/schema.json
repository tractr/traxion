{
  "$id": "PwaApp",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "apiName": {
      "default": "api",
      "description": "Configure the api name that you use in your workspace (default: api)",
      "type": "string"
    },
    "name": {
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "default": "pwa",
      "description": "The name of the pwa app that will be create",
      "type": "string",
      "x-prompt": "What name would you like to use for your pwa ? (default: pwa)"
    },
    "skipInstall": {
      "default": false,
      "description": "Skip installing dependencies",
      "type": "boolean"
    }
  },
  "required": [
    "name"
  ],
  "title": "",
  "type": "object"
}