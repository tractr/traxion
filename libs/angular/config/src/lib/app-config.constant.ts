import { InjectionToken } from '@angular/core';

export const APP_CONFIG_OPTIONS = new InjectionToken('app.config.options');
export const APP_CONFIG_SERVICE = new InjectionToken('app.config.service');
