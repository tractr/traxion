version: '3'

services:
  
  minio:
    image: minio/minio
    command: server /export
    ports:
      - 9000:9000
    environment:
      MINIO_ACCESS_KEY: "minio"
      MINIO_SECRET_KEY: "password"
    volumes:
      - s3-data:/export

volumes:
  s3-data: