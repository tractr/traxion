
import { ValidationErrors } from '@angular/forms';

import { Field } from '@trxn/hapify-core';

// TODO: implements a validators on fields
export const composer = (field: Field): ValidationErrors => []