import { Injectable } from "@angular/core";
import { ValidatorFn } from "@angular/forms";
import { required } from "@poc/trxn-angular-tools";

@@NgModule({
      imports: [
        CommonModule,
        ReactiveFormsModule,
        FormsModule,
        AngularModelsServicesModule,
      ],
    })
export class AngularModelsValidatorsModule {
}
