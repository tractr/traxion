import { terraformAwsComponent } from './terraform-aws-component';

describe('terraformAwsComponent', () => {
  it('should work', () => {
    expect(terraformAwsComponent()).toEqual('terraform-aws-component');
  });
});
