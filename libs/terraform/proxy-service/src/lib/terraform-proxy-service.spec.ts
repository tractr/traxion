import { terraformProxyService } from './terraform-proxy-service';

describe('terraformProxyService', () => {
  it('should work', () => {
    expect(terraformProxyService()).toEqual('terraform-proxy-service');
  });
});
