###########################
##      Stack tests      ##
###########################
name: Stack Tests

# Start the job on all pull request
on:
  pull_request:

jobs:
  tests:
    name: Stack Tests
    uses: ./.github/workflows/test.yml
    with:
      checkout: true
