###########################
##      Stack tests      ##
###########################
name: Test

# Start the job on all pull request
on:
  pull_request:

jobs:
  test:
    uses: tractr/stack/.github/workflows/shared/test.yml
